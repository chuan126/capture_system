diff --git a/frontend/README.md b/frontend/README.md
index 8d3067e..4b049db 100644
--- a/frontend/README.md
+++ b/frontend/README.md
@@ -1,8 +1,8 @@
 # 隧道净空测量显控终端前端
 
 ODIN1 Lite车载隧道净空高度测量系统的浏览器界面。采集首页已经接入Three.js
-雷达局部东北天点云预览、RTK定位详情、实时最低顶面高度和统一系统状态，其他任务、
-回放和报告业务仍是静态界面。
+雷达局部东北天点云预览、高德实时地图、RTK定位详情、实时最低顶面高度和统一系统
+状态，其他任务、回放和报告业务仍是静态界面。
 
 ## 实时最低顶面高度
 
@@ -36,6 +36,24 @@ RMC状态、当前坐标和高度。页面只把协议状态码翻译为文字
 窗口或进出洞结论。`NavSatFix.status`为无定位时，当前坐标和高度显示为`--`；入口
 和出口坐标继续等待任务定位模块提供。
 
+## 高德实时地图
+
+采集首页直接复用同源`/ws/v1/rtk`快照。RTK有效时，前端通过高德JS API 2.0将
+WGS84经纬度转换为GCJ-02，绘制车辆位置与最多2,500个轨迹点。RTK无定位或RMC
+明确无效时，地图保留最后有效位置，不增加轨迹点。
+
+首次使用时点击“地图设置”，填写高德Web端JS API Key和`securityJsCode`。配置保存在
+当前浏览器的`localStorage`，后续打开页面会自动加载。构建时也可通过以下环境变量
+提供初始值：
+
+```bash
+NEXT_PUBLIC_AMAP_KEY=你的Web端Key
+NEXT_PUBLIC_AMAP_SECURITY_CODE=你的securityJsCode
+```
+
+正式部署应在高德控制台限制允许访问的域名。更换Key或安全密钥后，页面会销毁旧地图
+实例并重新加载JS API。
+
 ## 系统状态
 
 采集首页通过同源`/ws/v1/system-status`显示雷达设备接入、RTK串口诊断、RK3588
diff --git a/frontend/app/globals.css b/frontend/app/globals.css
index 7d05cfb..bd649d4 100644
--- a/frontend/app/globals.css
+++ b/frontend/app/globals.css
@@ -372,7 +372,7 @@ th { color: #7f8b9e; background: #fafbfd; font-weight: 600; white-space: nowrap;
 
 .dashboard-layout {
   display: grid;
-  grid-template-columns: minmax(0, 1.92fr) minmax(350px, .88fr);
+  grid-template-columns: minmax(0, 1fr) clamp(330px, 18vw, 344px);
   align-items: stretch;
   gap: 12px;
   min-height: 0;
@@ -381,13 +381,201 @@ th { color: #7f8b9e; background: #fafbfd; font-weight: 600; white-space: nowrap;
 .dashboard-main, .dashboard-side { display: grid; min-height: 0; gap: 10px; }
 .dashboard-main { grid-template-rows: minmax(0, 1fr) 168px; }
 .dashboard-side { grid-template-rows: minmax(0, 1fr); }
-.dashboard-main > .panel { display: flex; min-height: 0; flex-direction: column; padding: 14px; }
+.dashboard-visual-grid {
+  display: grid;
+  grid-template-columns: minmax(0, 1.04fr) minmax(0, .96fr);
+  gap: 10px;
+  min-height: 0;
+}
+.dashboard-main > .panel,
+.dashboard-visual-grid > .panel { display: flex; min-height: 0; flex-direction: column; padding: 14px; }
 .dashboard-main > .panel > .panel-head { margin-bottom: 9px; }
 .dashboard-main > .panel > .chart { flex: 1; min-height: 0; height: auto; }
 .dashboard-cloud-panel { position: relative; }
 .dashboard-cloud-panel .cloud-toolbar { flex: 0 0 auto; margin-bottom: 9px; }
 .dashboard-cloud-panel .dashboard-cloud-stage { flex: 1; min-height: 0; }
 
+
+/* 采集首页实时地图 */
+.dashboard-map-panel { position: relative; overflow: hidden; }
+.map-panel-head {
+  display: flex;
+  flex: 0 0 auto;
+  align-items: flex-start;
+  justify-content: space-between;
+  gap: 12px;
+  margin-bottom: 9px;
+}
+.map-panel-head h2 { margin: 0; font-size: 15px; }
+.map-panel-head p { margin: 5px 0 0; color: var(--muted); font-size: 10px; }
+.map-settings-button {
+  flex: 0 0 auto;
+  min-height: 30px;
+  padding: 6px 10px;
+  color: var(--primary);
+  border: 1px solid #cfdeff;
+  border-radius: 8px;
+  background: #f2f6ff;
+  font-size: 10px;
+  font-weight: 700;
+}
+.map-settings-button:hover { background: #e9f1ff; }
+.amap-stage {
+  position: relative;
+  flex: 1;
+  min-height: 0;
+  overflow: hidden;
+  border: 1px solid #dce4ef;
+  border-radius: 12px;
+  background: #eef3f8;
+}
+.amap-container { position: absolute; inset: 0; width: 100%; height: 100%; }
+.amap-empty {
+  position: absolute;
+  z-index: 3;
+  inset: 0;
+  display: grid;
+  place-content: center;
+  justify-items: center;
+  gap: 7px;
+  padding: 24px;
+  color: #718096;
+  text-align: center;
+  background:
+    linear-gradient(rgba(255,255,255,.88), rgba(255,255,255,.88)),
+    repeating-linear-gradient(45deg, #eaf0f6 0, #eaf0f6 12px, #f6f8fb 12px, #f6f8fb 24px);
+}
+.amap-empty > span {
+  display: grid;
+  width: 40px;
+  height: 40px;
+  place-items: center;
+  color: var(--primary);
+  border-radius: 11px;
+  background: var(--primary-light);
+  font-size: 13px;
+  font-weight: 800;
+}
+.amap-empty strong { color: #405069; font-size: 12px; }
+.amap-empty small { max-width: 310px; color: #8290a3; font-size: 10px; line-height: 1.55; }
+.amap-empty button {
+  min-height: 31px;
+  margin-top: 2px;
+  padding: 6px 11px;
+  color: #fff;
+  border: 0;
+  border-radius: 8px;
+  background: var(--primary);
+  font-size: 10px;
+  font-weight: 700;
+}
+.amap-status-row {
+  position: absolute;
+  z-index: 2;
+  top: 12px;
+  left: 12px;
+  display: flex;
+  gap: 6px;
+  flex-wrap: wrap;
+  pointer-events: none;
+}
+.amap-status-row > span {
+  display: inline-flex;
+  align-items: center;
+  gap: 5px;
+  padding: 6px 8px;
+  color: #68768a;
+  border: 1px solid rgba(220,228,239,.95);
+  border-radius: 999px;
+  background: rgba(255,255,255,.92);
+  font-size: 9px;
+  box-shadow: 0 4px 12px rgba(44,63,92,.06);
+  backdrop-filter: blur(7px);
+}
+.amap-status-row strong { color: #34445b; font-size: 9px; }
+.amap-coordinate {
+  position: absolute;
+  z-index: 2;
+  right: 12px;
+  bottom: 12px;
+  left: 12px;
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 12px;
+  min-width: 0;
+  padding: 7px 9px;
+  color: #526177;
+  border: 1px solid rgba(220,228,239,.95);
+  border-radius: 8px;
+  background: rgba(255,255,255,.9);
+  font-size: 9px;
+  box-shadow: 0 4px 12px rgba(44,63,92,.06);
+  backdrop-filter: blur(7px);
+  pointer-events: none;
+}
+.amap-coordinate span { flex: 0 0 auto; font: 700 9px/1 Arial; }
+.amap-coordinate small { overflow: hidden; color: #7b8899; font-size: 8px; text-overflow: ellipsis; white-space: nowrap; }
+.amap-vehicle-marker {
+  width: 42px;
+  height: 58px;
+  transform-origin: center;
+  filter: drop-shadow(0 6px 8px rgba(20,49,98,.27));
+  pointer-events: none;
+}
+.amap-vehicle-marker svg { display: block; width: 100%; height: 100%; overflow: visible; }
+.amap-vehicle-marker__heading { fill: #1769ee; opacity: .72; }
+.amap-vehicle-marker__body rect:first-child { fill: #1769ee; stroke: #fff; stroke-width: 2; }
+.amap-vehicle-marker__body path:nth-of-type(1) { fill: #d8ecff; }
+.amap-vehicle-marker__body path:nth-of-type(2) { fill: #b9d7f4; }
+.amap-vehicle-marker__body rect:not(:first-child) { fill: #27364c; }
+.map-modal-mask {
+  position: fixed;
+  z-index: 200;
+  inset: 0;
+  display: grid;
+  place-items: center;
+  padding: 20px;
+  background: rgba(23,32,51,.34);
+  backdrop-filter: blur(4px);
+}
+.map-modal-panel {
+  display: grid;
+  gap: 13px;
+  width: min(540px, 100%);
+  padding: 20px;
+  border: 1px solid var(--line);
+  border-radius: 16px;
+  background: #fff;
+  box-shadow: 0 24px 70px rgba(28,50,90,.22);
+}
+.map-modal-head { display: flex; align-items: flex-start; justify-content: space-between; gap: 16px; }
+.map-modal-head h2 { margin: 0; font-size: 17px; }
+.map-modal-head p { margin: 6px 0 0; color: var(--muted); font-size: 11px; }
+.map-modal-head > button {
+  width: 30px;
+  height: 30px;
+  padding: 0;
+  color: #66758a;
+  border: 0;
+  border-radius: 8px;
+  background: #edf2f8;
+  font-size: 18px;
+}
+.map-modal-panel label > span { margin-bottom: 6px; font-size: 11px; }
+.map-modal-panel input { height: 40px; font-size: 11px; }
+.map-security-note {
+  padding: 10px 11px;
+  color: #67768a;
+  border: 1px solid var(--line-soft);
+  border-radius: 9px;
+  background: #f8fafc;
+  font-size: 10px;
+  line-height: 1.6;
+}
+.map-modal-actions { display: flex; justify-content: flex-end; gap: 8px; }
+.map-modal-actions .button { min-width: 98px; }
+
 .current-task { overflow: hidden; }
 .task-identity { display: grid; grid-template-columns: 1fr 1fr; gap: 1px; overflow: hidden; border: 1px solid var(--line-soft); border-radius: 10px; background: var(--line-soft); }
 .task-identity > div { display: grid; gap: 5px; min-height: 52px; padding: 10px; background: #fbfcfe; }
@@ -769,6 +957,8 @@ table {
   .report-layout { grid-template-columns: 300px minmax(0, 1fr); }
   .report-basic-info { grid-template-columns: repeat(3, minmax(0, 1fr)); }
   .dashboard-main, .dashboard-side { grid-template-rows: none; }
+  .dashboard-main { grid-template-rows: minmax(380px, 1fr) 168px; }
+  .dashboard-visual-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }
   .operation-panel { height: auto; overflow: visible; }
   .dashboard-health-bar { grid-template-columns: 1fr; }
   .health-devices { border: 0; }
@@ -811,7 +1001,10 @@ table {
   .cloud-toolbar { grid-template-columns: 1fr; }
   .cloud-toolbar .panel-tag { position: absolute; top: 17px; right: 17px; }
   .cloud-stage { min-height: 450px; }
+  .dashboard-visual-grid { grid-template-columns: 1fr; }
+  .dashboard-main { grid-template-rows: none; }
   .dashboard-cloud-panel .dashboard-cloud-stage { flex-basis: 340px; min-height: 340px; }
+  .amap-stage { flex: 0 0 340px; min-height: 340px; }
   .dashboard-main > .panel > .chart { flex-basis: 190px; min-height: 190px; height: 190px; }
   .cloud-stage__footer { align-items: flex-start; flex-direction: column; gap: 5px; }
   .operation-panel .tunnel-markers { grid-template-columns: 1fr; }
diff --git a/frontend/app/page.tsx b/frontend/app/page.tsx
index b9d6a25..430c314 100644
--- a/frontend/app/page.tsx
+++ b/frontend/app/page.tsx
@@ -4,6 +4,7 @@ import { useEffect, useMemo, useState } from "react";
 
 import { useClearanceSocket } from "@/components/clearance/useClearanceSocket";
 import type { ClearanceSnapshot } from "@/components/clearance/clearanceProtocol";
+import RealtimeAmap from "@/components/map/RealtimeAmap";
 import PointCloudViewer from "@/components/point-cloud/PointCloudViewer";
 import type { PointCloudViewMode } from "@/components/point-cloud/PointCloudViewer";
 import { useRtkSocket } from "@/components/rtk/useRtkSocket";
@@ -348,31 +349,39 @@ function Dashboard() {
 
       <section className="dashboard-layout">
         <div className="dashboard-main">
-          <article className="panel cloud-panel dashboard-cloud-panel">
-            <div className="cloud-toolbar">
-              <div>
-                <h2>点云实时预览</h2>
-              </div>
-              <div className="segmented segmented--small" aria-label="点云视角">
-                {modes.map((mode) => (
-                  <button
-                    type="button"
-                    key={mode.id}
-                    className={viewMode === mode.id ? "active" : ""}
-                    disabled={mode.disabled}
-                    title={mode.disabled ? "等待真实断面接口接入" : undefined}
-                    onClick={() => setViewMode(mode.id)}
-                  >
-                    {mode.label}
-                  </button>
-                ))}
+          <section className="dashboard-visual-grid" aria-label="点云与实时地图">
+            <article className="panel cloud-panel dashboard-cloud-panel">
+              <div className="cloud-toolbar">
+                <div>
+                  <h2>点云实时预览</h2>
+                </div>
+                <div className="segmented segmented--small" aria-label="点云视角">
+                  {modes.map((mode) => (
+                    <button
+                      type="button"
+                      key={mode.id}
+                      className={viewMode === mode.id ? "active" : ""}
+                      disabled={mode.disabled}
+                      title={mode.disabled ? "等待真实断面接口接入" : undefined}
+                      onClick={() => setViewMode(mode.id)}
+                    >
+                      {mode.label}
+                    </button>
+                  ))}
+                </div>
+                <span className="panel-tag panel-tag--muted">预览链路</span>
               </div>
-              <span className="panel-tag panel-tag--muted">预览链路</span>
-            </div>
-            <PointCloudViewer viewMode={viewMode as PointCloudViewMode} />
-          </article>
+              <PointCloudViewer viewMode={viewMode as PointCloudViewMode} />
+            </article>
 
-          <article className="panel">
+            <RealtimeAmap
+              snapshot={rtkSnapshot}
+              hasFix={hasFix && rmcCharacter !== "V"}
+              connectionDetail={rtk.detail}
+            />
+          </section>
+
+          <article className="panel dashboard-clearance-panel">
             <PanelHead
               title="实时净空高度曲线"
               description="最近120帧有效高度与无效帧断点"
diff --git a/frontend/package.json b/frontend/package.json
index a3c3166..e5ecf12 100644
--- a/frontend/package.json
+++ b/frontend/package.json
@@ -10,7 +10,7 @@
     "build": "WRANGLER_LOG_PATH=.wrangler/wrangler.log vinext build",
     "build:device": "CAPTURE_DEVICE_EXPORT=1 next build",
     "start": "WRANGLER_LOG_PATH=.wrangler/wrangler.log vinext start",
-    "test": "npm run build && node --experimental-strip-types --test tests/rendered-html.test.mjs tests/cloud-preview-protocol.test.mjs tests/rtk-protocol.test.mjs tests/system-status-protocol.test.mjs tests/clearance-protocol.test.mjs",
+    "test": "npm run build && node --experimental-strip-types --test tests/amap-interface.test.mjs tests/rendered-html.test.mjs tests/cloud-preview-protocol.test.mjs tests/rtk-protocol.test.mjs tests/system-status-protocol.test.mjs tests/clearance-protocol.test.mjs",
     "test:device": "npm run build:device && node --test tests/static-export.test.mjs",
     "lint": "eslint . --ignore-pattern dist --ignore-pattern .next"
   },
