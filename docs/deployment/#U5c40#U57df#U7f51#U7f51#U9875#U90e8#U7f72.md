# RK3588 局域网 Web 部署

核对日期：2026-08-06


> 当前边界：任务创建和查询已经写入设备端 SQLite。开始、暂停和停止仍只修改浏览器
> 内存状态，尚未连接设备端 `task_manager`。数据回放可以读取已有测量文件，正式 TXT 和 PDF 文件由 FastAPI 生成。设备端记录节点仍未实现。

## 架构

前端在构建阶段导出为静态文件。RK3588 运行时只启动一个 FastAPI/Uvicorn
服务，由同一个端口提供：

- `/`：前端静态界面；
- `/api/health`：Web 服务健康检查；
- `/api/v1/tasks`：任务创建和持久化任务查询；
- `/api/v1/tasks/batch`：多任务事务创建；
- `/api/v1/tasks/{task_id}`：单任务查询和逻辑删除；
- `/api/v1/tasks/{task_id}/measurements`：历史测量文件读取；
- `/api/v1/reports/clearance-summary/preview`：读取正式导出资格和汇总预览；
- `/api/v1/tasks/{task_id}/exports/txt`：生成任务 50 Hz TXT；
- `/api/v1/reports/clearance-summary`：生成正式任务 PDF 汇总；
- `/ws/v1/cloud-preview`：PCV1点云WebSocket；
- `/ws/v1/clearance`：单帧净空结果WebSocket；
- `/ws/v1/rtk`：RTK状态和fix快照WebSocket；
- `/ws/v1/system-status`：统一系统状态WebSocket。

浏览器不直接连接 ROS 2。任务 HTTP 接口只访问 FastAPI 和 SQLite。网络断开不会停止
已经启动的 ROS 2 节点；正式设备端任务状态机尚未实现。

## 环境准备

在项目根目录创建独立 Python 环境并安装后端依赖：

```bash
cd /home/cat/Project/capture_system
python3 -m venv .venv
.venv/bin/python -m pip install -r backend/requirements.txt
sudo apt-get install fonts-arphic-gbsn00lp
```

首次准备前端依赖：

```bash
cd /home/cat/Project/capture_system/frontend
npm ci
```

## 构建前端

```bash
cd /home/cat/Project/capture_system
scripts/build/build_web.sh
```

构建成功后必须存在：

```text
/home/cat/Project/capture_system/frontend/out/index.html
```

如果该文件不存在，FastAPI 会拒绝启动并明确报告静态构建缺失，避免设备提供
不完整页面。

## 手动运行

开发阶段推荐直接使用一键前台脚本：

```bash
cd /home/cat/Project/capture_system
scripts/operation/run_lan_preview.sh
```

该脚本会依次启动雷达驱动、点云预览节点、首版净空计算节点、RTK驱动、系统
监控和网页服务，并打印所有可访问的局域网地址。按 `Ctrl+C` 会统一停止本次启动
的进程，不安装或启用任何开机自启服务。室内没有有效定位时，RTK节点仍保持运行；RTK驱动通过`/diagnostics`报告
串口事实，`system_monitor`将其与雷达、RK3588和实际数据存储状态统一发布到
`/capture/system/diagnostics`。

需要分别调试组件时，可先加载业务工作空间并启动点云预览节点：

```bash
source /opt/ros/humble/setup.bash
source /home/cat/Project/capture_system/ros2_ws/install/setup.bash
ros2 launch bringup cloud_preview.launch.py
```

单独调试首版净空计算节点：

```bash
ros2 launch bringup clearance_preview.launch.py
```

该入口启动高频里程计时间适配、逐点运动补偿和净空算法，输入最终为 `/capture/lidar/points_compensated_enu`。

单独调试RTK驱动：

```bash
ros2 launch rtk_driver rtk_driver.launch.py
```

雷达驱动由系统采集Launch或当前 `sensor_adapter` 入口启动。确认以下Topic存在：

```bash
ros2 topic info /capture/visualization/cloud_preview
```

再启动Web服务：

```bash
cd /home/cat/Project/capture_system
scripts/operation/run_web.sh
```

默认监听所有 IPv4 网卡的 TCP 8000 端口。在 RK3588 上查询地址：

```bash
ip -br address
```

假设有线地址为 `192.168.1.200`，同一网段电脑访问：

```text
http://192.168.1.200:8000/
```

健康检查：

```bash
curl http://192.168.1.200:8000/api/health
```

正常响应为：

```json
{"status":"ok"}
```

采集首页点云面板只保留三维视图和放大按钮，不再显示旧版帧数、FPS和frame底部状态栏。可在浏览器开发者工具或ROS 2 Topic中检查链路。

## 网络参数

参数文件是 `config/network/web.env`：

| 参数 | 单位/格式 | 默认值 | 合法范围 | 失效行为 |
| --- | --- | --- | --- | --- |
| `UVICORN_HOST` | IPv4 地址 | `0.0.0.0` | 本机有效监听地址 | 地址不可用时服务启动失败 |
| `UVICORN_PORT` | TCP 端口 | `8000` | `1`–`65535`，且未被占用 | 端口无效或被占用时服务启动失败 |
| `CAPTURE_STATIC_DIR` | 绝对目录 | `frontend/out` 的绝对路径 | 必须包含 `index.html` | 服务启动失败并报告缺失文件 |
| `CAPTURE_DATA_ROOT` | 绝对目录 | `/home/cat/.local/share/capture_system` | 运行用户可创建和写入 | 启动脚本拒绝启动，任务 API 不可用 |

`0.0.0.0` 表示同时监听有线和 Wi-Fi 网卡。它不是浏览器访问地址；浏览器必须
使用 RK3588 在相应局域网内的实际 IP。

Web启动脚本会先创建 `CAPTURE_DATA_ROOT/tasks` 并检查写权限，再依次加载ROS 2、厂商
驱动工作空间和业务工作空间，固定使用一个
Uvicorn worker，并关闭点云WebSocket压缩。`.venv`必须安装
`backend/requirements.txt` 中的NumPy，否则ROS消息类型无法载入，页面会显示
“ROS桥不可用”。

## systemd开机启动（当前阶段不启用）

当前开发阶段使用 `run_lan_preview.sh` 前台运行，不执行下列安装和启用操作。
本节仅保留为后续设备部署参考。

安装服务单元：

```bash
sudo install -m 0644 \
  system/systemd/capture-web.service \
  /etc/systemd/system/capture-web.service
sudo systemctl daemon-reload
sudo systemctl enable --now capture-web.service
```

查看状态和日志：

```bash
systemctl status capture-web.service
journalctl -u capture-web.service -f
```

更新前端后重新构建并重启：

```bash
scripts/build/build_web.sh
sudo systemctl restart capture-web.service
```

## 测试

开发测试环境需要额外依赖：

```bash
.venv/bin/python -m pip install -r backend/requirements-dev.txt
scripts/build/test_web.sh
```

测试脚本会验证：

- Sites/Vinext 构建和服务端渲染测试；
- RK3588 静态导出及静态页面内容；
- PCV1浏览器解析和边界拒绝；
- FastAPI ROS桥状态、最新帧队列、同源WebSocket和二进制发送；
- RTK ROS桥、最新值覆盖、同源WebSocket和5 Hz发送上限；
- 净空和系统状态协议、WebSocket、任务 SQLite 持久化接口以及采集首页任务控制源码结构；
- 前端 lint；
- FastAPI 首页、静态资源、健康检查和缺失构建保护。

Web 后端测试会禁用 pytest 自动加载的 ROS 2 插件，只隔离测试发现过程，不会
修改或运行任何 ROS 2 节点。

## 常见状态

| 页面状态 | 检查方向 |
| --- | --- |
| ROS桥不可用 | 确认使用 `run_web.sh` 启动且NumPy已安装 |
| WebGL初始化失败 | 在电脑浏览器启用硬件加速或更换支持WebGL的浏览器 |
| 客户端数量达到上限 | 关闭其他点云页面，当前最多四个客户端 |
