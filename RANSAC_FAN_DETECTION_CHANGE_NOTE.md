# 单帧RANSAC风机底面检测修改说明

## 修改目标

在60 km/h车载环境下，以ODIN约10 Hz点云逐帧检测当前车道或相邻车道上方的风机底层平面，不进行多帧点云累积。

## 约束落实

1. `third_party/odin_ros_driver`已恢复为最初上传版本，未修改厂商驱动源码。
2. 逐点坐标转换采用

```text
r_n,i = R_n<-b(t_i) r_l,i + p_n(t_i) - p_n(t_0)
```

`C0`已从参数、类接口、计算和测试中删除。雷达坐标系与里程计机体系方向一致，杆臂为0。
3. 厂商高频里程计的重复时间戳在业务侧`odometry_timestamp_adapter_node`处理。厂商高频里程计先重映射到`/capture/odometry/high_rate_raw`，适配后发布`/capture/odometry/high_rate`。
4. `clearance_engine`保留单帧多平面RANSAC，不使用多帧点云累积。

## RANSAC修改

- 顶部ROI扩大到两个水平轴各60°，覆盖当前车道和相邻车道。
- 高度范围调整为1至10 m。
- 最大候选平面数由8降至4。
- 最大迭代次数由500降至200。
- 增加4 cm体素降采样，仅用于RANSAC模型搜索。
- 候选模型回到原始分辨率点云收集内点。
- 原始点云用于连通区域、PCA重新拟合、残差和最低高度计算。
- 区域网格使用3 cm，最小双向跨度为4格，最少占用12格。
- 当前平面未通过区域检查时继续搜索后续平面，不再直接结束。
- 净空节点订阅队列深度设为1，只处理最新帧。

## 验证状态

已完成YAML解析、Python Launch语法检查、运动补偿核心C++独立编译、厂商驱动逐文件一致性检查和代码静态核对。当前环境未安装ROS 2 Humble、PCL和Eigen，未执行完整`colcon build`及实车测试。
