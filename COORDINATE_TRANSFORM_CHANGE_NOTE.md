# 车载动态坐标转换修改说明

## 修改后的计算关系

第`i`个雷达点采用逐点里程计姿态计算。

```text
r_n,i = R_n<-b(t_i) × C0_b<-l × r_l,i
```

`R_n<-b`由里程计ROS四元数`[x,y,z,w]`插值后转换得到。`C0`为雷达坐标系到
里程计机体坐标系的固定旋转矩阵，默认值为`diag(-1,-1,1)`。导航坐标系第三轴为Up，
因此转换结果第三分量直接作为雷达原点到目标点的竖直分量。

## 删除的处理

- 删除IMU加速度插值。
- 删除加速度模长7至12 m/s²检查。
- 删除依据瞬时加速度方向构造的重力对齐矩阵。
- 删除运动补偿节点对IMU Topic的订阅和时间覆盖等待。
- 删除初始化航向归零矩阵`Cenu_odom`。

## 保留的处理

- 点云中每个点的`offset_time`。
- 里程计四元数SLERP插值。
- 可选的里程计位置平移补偿。
- 姿态缓存、最大插值间隔和空点云错误输出。

## 配置变化

删除参数

```text
imu_topic
min_gravity_norm_m_s2
max_gravity_norm_m_s2
```

增加参数

```text
lidar_to_odometry_rotation
```

该参数必须包含9个按行排列的数值，并构成正交、行列式为1的旋转矩阵。
