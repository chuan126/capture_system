# AGENTS.md

本文件适用于 `capture_system/` 目录及其全部子目录，用于指导后续开发者和
自动化开发代理在不破坏总体架构的前提下持续实现本系统。

## 1. 系统目标

本系统运行在 RK3588 上，接入 ODIN1 Lite 激光雷达和 RTK，实现：

1. 车辆以最高 60 km/h 行驶时持续采集传感器数据；
2. 在进入和离开隧道时记录可靠的 RTK 坐标及定位质量；
3. 在隧道内完成点云运动补偿、断面提取和实时净空高度计算；
4. 在 RK3588 本地完成全部采集、计算、存储和控制；
5. 电脑仅通过 Wi-Fi 或网口访问浏览器界面进行监视和控制。

开发时优先保证数据正确性、时间一致性、可诊断性和故障安全，不以界面效果
代替核心测量能力。

## 1.1 当前实现边界

截至 2026-08-07，`task_manager` 和 `data_recorder` 已实现首版。任务创建、状态和参数快照
保存在设备端 SQLite。前端显示编号由任务创建时间生成，格式为 `YYYYMMDD_HHMMSS`；同秒
创建多项任务时追加 `_02`、`_03`。内部状态、控制和文件目录始终使用稳定 UUID。开始、
暂停、继续和停止由浏览器调用 FastAPI，再转发到 ROS 2 Service。浏览器不得直接连接 ROS 2，
也不得本地确认设备端状态转换。

开始流程不等待雷达或RTK真实数据检查。入口和出口RTK使用按钮时最新快照，未确认时
保存状态并继续，不等待人工确认。`data_recorder`保存净空实际源帧和50 Hz最近源帧
保持序列。重复记录必须保存来源字段，不能描述为50 Hz独立传感器测量。

网页点云预览使用 `/capture/lidar/points_compensated_enu`，不使用厂商 SLAM 点云。
数据回放和正式导出通过 FastAPI 读取每任务测量 SQLite 文件。PDF 汇总用户显式选择的
任务。回放页只提供逻辑删除和批量选择，不向客户暴露物理清理入口；物理数据清理接口保留给维护流程。
历史批次字段和接口只用于旧数据库兼容，不作为当前前端工作流。

## 2. 目录与模块边界

- `third_party/`：第三方驱动和依赖源码。
- `ros2_ws/src/interfaces/`：系统自定义消息、服务和 Action。
- `ros2_ws/src/rtk_driver/`：RTK 串口通信、NMEA 解析和质量状态。
- `ros2_ws/src/sensor_adapter/`：通过 ROS 2 remapping 将厂商 Topic 映射为稳定
  的 `/capture/...` Topic，并提供雷达与 RViz2 启动入口。
- `ros2_ws/src/motion_compensation/`：时间检查、位姿缓存、插值和点云去畸变。
- `ros2_ws/src/localization/`：入口出口判断、洞内相对里程和轨迹估计。
- `ros2_ws/src/clearance_engine/`：点云处理、路面建模、断面和净空计算。
- `ros2_ws/src/task_manager/`：任务生命周期、RTK端点状态和记录器编排。
- `ros2_ws/src/data_recorder/`：原始数据、任务元数据和计算结果记录。
- `ros2_ws/src/cloud_visualization/`：浏览器预览数据的降采样和编码。
- `ros2_ws/src/system_monitor/`：传感器、Topic、算法和系统资源监控。
- `ros2_ws/src/bringup/`：系统 Launch、节点组合和 QoS 配置。
- `backend/`：FastAPI、WebSocket 和 ROS 2 Web 桥接。
- `frontend/`：浏览器监视与控制界面。
- `config/`：部署参数和现场覆盖配置，不保存算法实现。
- `tools/`：标定、分析、仿真和导出工具。
- `tests/`：系统集成、数据回放、性能和现场测试。

新增功能应放入职责最接近的现有模块。只有在职责确实独立、依赖边界明确时才
新增 ROS 2 包，避免一个小功能对应一个新包。

## 3. 第三方驱动规则

`third_party/odin_ros_driver/src/odin_ros_driver2` 是从现有 SLAM 工程复制的
厂商驱动，默认视为只读。

- 业务代码不得直接调用 ODIN SDK 内部接口，应使用 ROS 2 标准消息。
- 厂商 Topic 命名差异统一在 `sensor_adapter` Launch 中处理。当前适配层不修改
  frame、字段和时间戳；相关语义必须由消费这些数据的核心模块显式检查。
- 不为业务需求直接修改厂商驱动。
- 如果驱动缺陷确实无法在适配层解决，先记录问题、复现条件和修改理由，再把
  补丁保持为最小变更，并在 `third_party/odin_ros_driver/README.md` 中登记。
- 不提交厂商目录内的 `.git`、`build`、`install`、`log` 或其他生成文件。

驱动当前主要输入接口为：

```text
/manifold/ODIN2/device0/cloud/raw
/manifold/ODIN2/device0/cloud/slam
/manifold/ODIN2/device0/imu
/manifold/ODIN2/device0/odometry
/manifold/ODIN2/device0/odometry_hf
```

实物型号是 ODIN1 Lite，但当前固件/SDK 2.0.2 上报的模型字符串是 `ODIN2`，
因此实际厂商 Topic 仍使用上述前缀。这是厂商接口兼容事实，不表示实物是
ODIN2。不得在其他业务模块中写死模型字符串和 `device0`；当前单设备前缀只可
作为 `sensor_adapter` Launch 参数配置，前缀变化时由部署配置显式覆盖。

## 4. ROS 2开发约定

- 目标环境为 Ubuntu 22.04、ROS 2 Humble、ARM64 RK3588。
- ROS 2 包名使用小写下划线，不增加统一的 `tunnel_` 前缀。
- 优先使用标准消息：`PointCloud2`、`Imu`、`NavSatFix`、`Odometry` 和 TF。
- 只有标准消息无法表达业务语义时，才在 `interfaces` 中新增自定义接口。
- 厂商 Topic 只能出现在驱动、适配层配置和适配层测试中。
- 业务 Topic 使用稳定的 `/capture/...` 命名空间。
- 每个 Topic 必须明确单位、坐标系、时间戳来源、频率和 QoS。
- 高频点云和位姿链路使用有界队列，禁止无界缓存。
- 参数写入 YAML；除协议常量外，不在算法中硬编码现场阈值。
- 节点负责通信和参数管理，核心算法应尽量保持为可独立单元测试的类或库。

建议 TF 树：

```text
map
└── odom
    └── base_link
        ├── lidar_link
        ├── imu_link
        └── rtk_link
```

不得仅根据厂商消息的 `frame_id` 假设点云坐标语义正确。外参和轴向必须通过
标定或实测确认。

## 5. 高速点云处理约束

60 km/h 约为 16.67 m/s。ODIN1 Lite 原始点云实测约 10.23 Hz，相邻帧间车辆
可能移动约 1.63 m；单帧 `offset_time` 实测覆盖 0–94.37 ms，帧内可能移动
约 1.57 m。

因此：

- 净空算法不得直接把整帧点云视为同一时刻。
- 核心输入优先使用带 `offset_time` 的原始点云。
- 使用 `odometry_hf` 和 IMU 建立带时间索引的位姿缓存。
- 每个点根据其采集时刻插值位姿，并补偿到统一参考时刻。
- 数据缺失、时间戳异常或位姿覆盖不足时必须降低结果置信度或输出无效结果，
  不得静默使用过期位姿。
- `cloud/slam` 只用于 RViz2、辅助诊断和历史验证。当前网页预览使用补偿后的局部东北天点云，不能用 SLAM 点云替代核心净空输入。
- 大点云处理路径避免不必要复制；优化前先以测量数据定位瓶颈。

任何涉及运动补偿的改动都应同时验证静止数据、动态回放和时间边界条件。

## 6. RTK与定位约束

- 复用已有 RTK NMEA 解析逻辑，不无理由重写协议解析器。
- 发布位置时必须同时保留定位解类型、卫星数、HDOP和可用误差指标。
- 当前任务控制优先满足高速作业实时性。开始和停止时立即保存最新RTK快照，不等待
  稳定窗口；无有效快照时保存`unconfirmed`，不阻塞命令。
- 后续若增加稳定端点推断，应作为记录后的质量处理，保留原始按钮时间和即时快照，
  不得延迟当前开始或停止流程。
- 当前 ODIN1 Lite 里程计动态测试出现约 14.3% 尺度低估和姿态漂移，不能把它当作
  已验证的精确洞内桩号。
- 实时洞内里程需要携带质量状态；出口 RTK 约束后的轨迹修正与实时估计应区分。
- 不把 ROS 消息中的全零协方差解释为零误差。

## 7. 净空结果约束

净空定义和车辆通行包络必须来自配置，并在任务开始时保存参数快照。

净空计算至少应考虑：

- 无效点、零点、非有限点和低置信度点过滤；
- 车辆本体及固定安装结构遮挡区域；
- 雷达到 `base_link` 的外参；
- 重力方向和车辆姿态；
- 路面基准模型；
- 指定通行包络内的最低顶部障碍物；
- 有效点率、路面质量、顶部质量和结果置信度。

无法可靠识别路面或顶部时输出“无效及原因”，不得沿用上一帧结果冒充当前值。
实时当前净空与任务累计最小净空应作为不同字段维护。

## 8. Web与系统边界

- 浏览器不直接连接 ROS 2，不处理原始点云，不承担核心计算。
- FastAPI 是对外的统一 HTTP 和 WebSocket 服务入口。
- `cloud_visualization` 只生成降采样或压缩后的预览数据，不再创建第二套外部
  WebSocket 服务，除非性能测试证明必须拆分。
- 控制命令经 backend 转换为 ROS 2 Service 或 Action。
- 后端不得复制任务状态机；任务真实状态以 `task_manager` 为准。
- 对浏览器限制点云频率和数据量，不能让前端连接影响实时计算链路。
- 网络中断不得终止 RK3588 上正在运行的采集和净空计算任务。

## 9. 数据与日志

- 当前结构化记录保存净空源帧、50 Hz最近源帧保持样本、RTK、端点快照、任务事件和
  暂停区间，不保存原始或补偿点云。
- 后续独立数据盘就绪后增加 `full_raw` 策略，原始 ROS 2 数据优先使用
  rosbag2/MCAP。
- `telemetry_only` 不能重放点云运动补偿和净空算法，最终算法验收仍必须使用
  另行采集并版本化的原始点云固定数据集。
- 任务元数据和结构化结果使用 SQLite，TXT 和 PDF 仅作为导出文件；旧批次表仅用于历史数据库兼容。
- 不将每帧点云保存成独立小文件。
- `data/`、`runtime/`、rosbag、MCAP、构建产物和日志不得提交到 Git。
- 每个任务必须保存任务 UUID、创建时间显示编号、软件版本、配置快照、标定版本、入口出口信息和
  诊断摘要。旧 `batch_id` 等字段只允许作为历史兼容信息，不得重新成为前端任务身份。
- 记录器必须保持为独立ROS 2进程，不得把SQLite写入放入点云处理节点。后续性能测试
  若发现记录器订阅积压，应引入有界写入队列或调整QoS，不得反压核心点云计算。
- 磁盘不足时按配置降级或停止记录，并明确告警；不得导致系统无响应。
- 日志不得包含密码、令牌或其他敏感信息。

## 10. 构建与运行

构建厂商驱动：

```bash
cd /home/cat/Project/capture_system/third_party/odin_ros_driver
source /opt/ros/humble/setup.bash
bash src/odin_ros_driver2/script/build_ros2.sh
```

验证厂商驱动：

```bash
source /opt/ros/humble/setup.bash
source /home/cat/Project/capture_system/third_party/odin_ros_driver/install/setup.bash
ros2 pkg executables odin_ros_driver_rev1
```

开发业务工作空间时，应先加载厂商驱动，再构建业务包：

```bash
source /opt/ros/humble/setup.bash
source /home/cat/Project/capture_system/third_party/odin_ros_driver/install/setup.bash
cd /home/cat/Project/capture_system/ros2_ws
colcon build --symlink-install
```

新建包后必须确保 `package.xml` 依赖完整，不能依赖开发主机上偶然存在但未声明的
包。

## 11. 测试要求

修改代码后按风险选择测试范围：

1. 协议解析、插值、坐标变换和净空核心算法必须有单元测试。
2. ROS 2接口变更必须有节点级或集成测试。
3. 点云算法改动必须使用固定数据集回放，并与基准结果比较。
4. 时间同步和运动补偿改动必须覆盖时间戳缺失、乱序、跳变和缓存边界。
5. Web控制改动必须验证浏览器断开后设备端任务继续运行。
6. 性能相关改动必须记录RK3588上的CPU、内存、处理延迟、队列积压和丢帧。
7. 不连接实机时，不声称完成了设备运行验证；明确区分编译、回放和实机测试。

不得为了让测试通过而放宽测量精度或静默忽略无效数据。

## 12. 开发工作方式

- 修改前先阅读目标模块、相邻接口、相关配置和现有测试。
- 新增或修改代码时，必要的代码注释统一使用中文；注释应说明设计意图、约束和
  非显然行为，避免重复描述显而易见的代码。
- 保留用户已有改动，不重置或覆盖无关文件。
- 优先做最小、完整、可验证的改动，避免顺便大规模重构。
- 修改公共接口前先检查所有发布者、订阅者、Web序列化和数据记录端。
- 新增参数时同时给出单位、默认值、合法范围和失效行为。
- 新增目录或模块时同步更新根目录 `README.md` 或相关架构文档。
- 新增、删除或调整可运行节点和 Launch 入口时，必须同步检查
  `scripts/operation/run_lan_preview.sh`、`bringup` 编排及相关部署文档是否需要更新。
- `docs/` 下的文档文件名使用中文；ROS 2、RTK、SLAM、MCAP 等必要技术缩写
  可以保留，但文件名的职责语义必须用中文表达。
- 系统自有文档只要展示文件或目录结构，树中的根目录、子目录和文件都必须在
  同一行末尾使用 `#` 添加中文职责注释；TF 树、数据流、Topic 链路和命令输出
  不属于文件结构，不按此规则添加注释。
- 不提交临时调试脚本、采集数据、二进制和构建目录。
- 不执行破坏性清理；需要删除用户数据、rosbag或标定结果时先征得用户确认。

## 13. 完成标准

一个开发任务只有在以下条件满足后才算完成：

- 功能放置在正确的模块边界内；
- 构建成功且没有新增未解释的警告；
- 相关自动化测试通过；
- 关键异常路径有明确行为和诊断信息；
- 配置、单位、坐标系和时间戳语义清楚；
- 不破坏厂商驱动只读边界；
- 不让Web、记录或预览链路阻塞核心实时计算；
- 必要的文档和使用方式已同步更新；
- 明确说明完成了编译验证、数据回放验证还是实机验证。

## 14. 架构图绘制规范

- 架构图和流程图不得出现交叉线，优先采用单向直线、分层链路或树形展开。
- Mermaid 流程图统一优先使用 `curve: linear`，并设置足够的节点和层级间距。
- 主流程在图中保持单一阅读方向；复杂回跳、异常转换和多对多关系改用表格说明。
- 避免双向箭头、虚线回跳和大量跨层连接；详细 Topic 发布订阅关系放入接口表。
- 一张图无法无交叉地表达全部关系时，应按职责或数据阶段拆成多张小图。
