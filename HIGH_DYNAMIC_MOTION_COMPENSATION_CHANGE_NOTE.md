# 高动态点云补偿修改说明

## 适用范围

本次修改针对高速和高加速度工况下的高度误差增大、补偿点云偶发为空。雷达、IMU和
里程计之间的杆臂平移按零处理，固定旋转外参仍为`C0`。

## 主要修改

1. 点云逐点转换采用

   ```text
   r_n,i = R_n<-b(t_i) C0 r_l,i + p_n(t_i) - p_n(t_0)
   ```

2. 启用扫描内相对平移，默认限制单帧相对位移不超过5米。
3. 姿态覆盖不到的点写为NaN，不再因单点失败清空整帧。
4. 点云尾部允许15毫秒姿态暂未覆盖，避免等待队列持续积压。
5. 等待队列增加到5帧。队列溢出和整帧失败默认不发布空点云。
6. 相同`offset_time`的点复用一次姿态计算，降低计算量。
7. 里程计订阅改为Reliable QoS，点云处理与里程计回调分开执行。
8. ODIN驱动为包内多个里程计样本生成独立时间戳，默认高频400 Hz、低频10 Hz。
9. 增加点云和里程计时间偏移参数，默认均为0秒。

## 需要实机确认的参数

- `odom_packet_timestamp_is_first_sample`
- `odometry_time_offset_s`
- `cloud_time_offset_s`
- `max_translation_per_scan_m`
- `minimum_valid_pose_ratio`

前两项需要结合厂商时间戳定义和动态数据标定。当前默认假设点云和里程计包头时间戳
均对应包内第一个样本。
